[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-trust-beacon"
version = "0.1.0"
description = "Trust beacon — emit, receive, aggregate trust signals across agents"
license = {text = "MIT"}
requires-python = ">=3.8"
